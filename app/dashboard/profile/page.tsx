// app/dashboard/layout.js
import React from "react";

export const metadata = {
  title: "Dashboard",
};

export default function DashboardLayout({ children }) {
  return (
      <div>
        {/* DashboardShell is a client component that manages sidebar state */}
        hello
      </div>
  );
}
